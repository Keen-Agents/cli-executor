# Task: Add Browser Automation to AgentOne Pipeline

You need to create/modify the following files to add Playwright-based browser automation. Follow the existing patterns EXACTLY.

## FILES TO CREATE

### File 1: `src/scripts/stages/browser-test.js`

Follow the EXACT pattern of `src/scripts/stages/test-suite.js`. Here's what test-suite.js looks like — your file must follow the same structure:

```javascript
import { readFileSync } from 'fs';
import { resolve, dirname } from 'path';
import { fileURLToPath } from 'url';
import { runAgent } from '../lib/agent-runner.js';
import { CostTracker } from '../lib/cost-tracker.js';
import { assembleContext } from '../lib/context-assembler.js';
import { TIMEOUTS } from '../pipeline-config.js';
```

The stage must:
- STAGE_NAME = 'browser-test'
- COMPLETED_REGEX = /<COMPLETED>([\s\S]*?)<\/COMPLETED>/
- Load prompt from '../prompts/browser-test.md'
- Get intake, implement, verify outputs from context.state.getStageOutput()
- Get plan via resolvePlan() helper (same as test-suite.js)
- Fill template with: TICKET_KEY, TICKET_SUMMARY, PLAN, IMPLEMENT_SUMMARY, VERIFY_SUMMARY, APP_URL, BRIDGE_URL
- For APP_URL: try to extract localhost URL from implement output, default to 'http://localhost:3000'
- Import BRIDGE_URL from '../pipeline-config.js'
- Spawn Claude agent with runAgent({ cli: 'claude', ... })
- Record costs via context.costTracker
- Parse pass/fail from content (check for 'pass' and no 'fail' in lowercase)
- Checkpoint output: { passed, summary, fullOutput, sessionId, durationMs, success }
- Handle errors with logger and re-throw

### File 2: `src/scripts/prompts/browser-test.md`

A markdown prompt template for the browser testing agent. Must include:
- Ticket info with {{TICKET_KEY}} and {{TICKET_SUMMARY}} placeholders
- {{PLAN}} and {{IMPLEMENT_SUMMARY}} placeholders
- {{VERIFY_SUMMARY}} placeholder
- Complete API reference for all bridge browser endpoints at {{BRIDGE_URL}}
- The 9 endpoints: launch, navigate, click, type, screenshot, content, evaluate, close, list
- With request body examples and response format
- Testing procedure steps
- Instructions to SKIP if not a web application
- COMPLETED tag with PASS/FAIL/SKIP format

### File 3: `src/scripts/tools/browser-automation.js`

Follow the pattern of the Keen framework tool. This tool allows agents to control a browser through the bridge API.

The tool must:
- Read mode from dictionary (launch/navigate/click/type/screenshot/content/evaluate/close)
- Read sessionId from dictionary
- Make fetch() calls to the bridge browser endpoints
- Set dictionary.response with the result
- Handle errors gracefully (set error message on dictionary.response)
- Use BRIDGE_URL and API_TOKEN from env vars (AGENTONE_BRIDGE_URL/BRIDGE_URL and AGENTONE_API_TOKEN/BRIDGE_API_TOKEN)

The tool MUST export `async function exec(dictionary)` — this is the Keen framework convention.
Use `dictionary.writeThinking` if available for logging, otherwise just console.log.

## FILES TO MODIFY

### File 4: `local-bridge.js`

This is a 1297-line file. You need to add browser session management. Here's what to add:

**At the top (after line 5, after the existing imports):**
```javascript
import { chromium } from 'playwright';
```

**After the session tracking variables (around line 265 area where `sessions` Map is defined), add:**
```javascript
const browserSessions = new Map();
const MAX_BROWSER_SESSIONS = 5;
const BROWSER_SESSION_TTL_MS = parseInt(process.env.BROWSER_SESSION_TTL_MS) || 10 * 60 * 1000;
```

**Add helper functions** (after the existing sessionSnapshot function):
- generateBrowserSessionId() — returns 'br-' + randomBytes(8).toString('hex')
- browserSessionSnapshot(bs) — returns { id, url, running, startedAt, closedAt, pagesOpened, screenshotsTaken, headless, label }
- closeBrowserSession(bs) — sets running=false, closedAt, closes page/context/browser with catch()

**In the existing TTL cleanup setInterval** (around line 379), add browser session cleanup too

**Add 9 browser endpoint handlers** as new `else if` blocks in the request handler (before the "Endpoint not found" fallback). Each endpoint must:
- Validate required params (throw Error if missing)
- Get browser session from browserSessions Map
- Use Playwright API to perform the action
- Return structured JSON response
- Console.log with timestamp for each operation

The endpoints:
1. `/api/browser/launch` — chromium.launch({headless}), newContext(), newPage(), store in Map
2. `/api/browser/navigate` — page.goto(url, {waitUntil, timeout}), return status+title
3. `/api/browser/click` — page.click(selector), waitForLoadState('networkidle')
4. `/api/browser/type` — page.type(selector, text, {delay}), optional clearFirst via page.fill
5. `/api/browser/screenshot` — page.screenshot() or locator.screenshot(), return base64
6. `/api/browser/content` — page.content() or page.innerText('body') or locator methods
7. `/api/browser/evaluate` — page.evaluate(expression)
8. `/api/browser/close` — closeBrowserSession(bs)
9. `/api/browser/list` — iterate browserSessions, return snapshots

**Also add GET /api/browser/list** in the GET handler section (around line 677-691).

**Update the startup banner** (around line 1288) to include BROWSER AUTOMATION section.

### File 5: `src/scripts/pipeline-config.js`

Current content has PROFILES, TIMEOUTS, AGENT_DEFAULTS. Make these changes:

1. Add 'browser-test' to PROFILES.standard.stages — insert AFTER 'test-suite' and BEFORE 'security-audit'
2. Add 'browser-test' to PROFILES.complex.stages — insert AFTER 'test-suite' and BEFORE 'security-audit'
3. Add timeout: `'browser-test': 600_000` to the TIMEOUTS object
4. Change AGENT_DEFAULTS.codex.model from 'gpt-5-codex' to 'gpt-5.4'

### File 6: `src/scripts/pipeline.js`

1. Add import at line 24: `import { run as browserTest } from './stages/browser-test.js';`
2. Add to STAGE_MAP: `'browser-test': browserTest`

### File 7: `keen-tools.json`

Currently has one tool entry. Add a second:
```json
{
    "name": "Browser Automation",
    "type": "script",
    "entry": "tools/browser-automation.js"
}
```

## IMPORTANT CONSTRAINTS

- This is an ES module project ("type": "module" in package.json)
- Use import/export, NOT require/module.exports
- local-bridge.js uses `import` syntax
- The bridge runs on port 3222, all endpoints require `x-api-token` header (already handled by the bridge's auth middleware)
- Playwright is already installed (`npm install playwright` + `npx playwright install chromium` done)
- All new src/ files must also be copied to dist/ (I'll handle this after you're done)
- Windows compatibility required (process.platform === 'win32' — already handled, Playwright works cross-platform)
- Follow the EXACT coding style of existing files (2-space indent, single quotes, semicolons)

## DO NOT

- Do NOT modify any file not listed above
- Do NOT add any other dependencies
- Do NOT create test files
- Do NOT create documentation files
- Do NOT change the bridge's auth mechanism
- Do NOT change the existing web scraping endpoints
